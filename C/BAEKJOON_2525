#include <stdio.h>

int main()
{   
    int hour = 0, min = 0, time = 0;
    scanf("%d %d", &hour, &min);
    scanf("%d", &time);
    
    min = min + time;
    hour = hour + min / 60;
    min = min % 60;
    hour = hour % 24;

    printf("%d %d", hour, min);
}  
