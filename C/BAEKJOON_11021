#include <stdio.h>
#include <stdlib.h>

int main()
{   
    int testCase = 0, a=0, b=0;
    scanf("%d", &testCase);

    for (int i = 0; i < testCase; i++) {
        scanf("%d %d", &a, &b);
        printf("Case #%d: %d\n", i + 1, a + b);
    }
    return 0;
}  
